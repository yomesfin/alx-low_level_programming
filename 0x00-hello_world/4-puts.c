#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always (Success)
 */
int main(void)
{
	char putstring[100] = "\"Programming is like building\
 a multilingual puzzle";

	puts(putstring);
	return (0);
}
